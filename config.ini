[SETTINGS]
estudios_por_pagina = 10
files_per_page = 10
analysis_items_per_page = 10
discrete_tables_per_page = 10
font_scale = 1.0
theme = Light
show_factory_reset_button = False
enable_hover_tooltips = False
max_automatic_backups = 4
# max_manual_backups is already in DEFAULT_SETTINGS, ensuring it's managed.
# No change needed here if it's correctly picked up from defaults or already present.
# Assuming it should be explicitly in the default config.ini content:
max_manual_backups = 4 
automatic_backup_cooldown_seconds = 60

